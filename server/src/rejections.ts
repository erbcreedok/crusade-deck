// Почему серверу пришлось отказать в действии. Молчаливый отказ недопустим: клиент
// показывает жест оптимистично, СРАЗУ — и без ответа карта так и осталась бы лежать не
// там, где на самом деле. На каждый отказ уходит код причины, а текст пишет клиент
// (двойник списка — client/src/game/rejections.ts).
//
// Раньше здесь жил ещё и flipRejectReason — правило для переворотов колоды. Перевороты
// существовали только вне «режима раздачи», а сам режим удалён вместе со второй механикой
// стола, поэтому и правило ушло.

export type RejectReason =
  // Режим свободы: карты со стола игроки берут сами, раздавать в чужие руки нельзя
  // никому, включая дилера.
  "free_mode";
